// This script runs before the build to ensure dependencies are correctly installed
console.log('Running prebuild script to ensure dependencies are correctly installed...');

// The actual work is done by pnpm install, which will update the lockfile
// This script is just a placeholder to ensure the prebuild step is executed